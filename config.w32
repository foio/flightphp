// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("flight", "for flight support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("flight", "enable flight support", "no");

if (PHP_FLIGHT != "no") {
	EXTENSION("flight", "flight.c");
}

